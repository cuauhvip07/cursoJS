const usuario = {
    nombre:'Carlos',
    apellido: 'Mendez',
    edad: 20,
}

console.log(Object.keys(usuario)) // Llaves del objeto
console.log(Object.values(usuario)) // Valor del objeto
console.log(Object.entries(usuario)) // Retorna todo en un arreglo sobre otro arreglo