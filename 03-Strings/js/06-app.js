const producto = 'Monitor 20 pulgadas'

// .repeat 


const promocion = 'en promocion '
console.log(promocion.repeat(3)) // Permite repetir una cadena de texto 
console.log(`${producto} ${promocion.repeat(3)}!!`)

// .split

const actividad = 'Aprendiendo JS moderno' 
console.log(actividad.split(' ')) //Separa en un arreglo lo que tu quieras separar

const productos = 'Melon, Papaya, Sandia, Piña'
console.log(productos.split(', '))